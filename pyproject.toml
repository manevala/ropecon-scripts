[project]
name = "ropecon-scripts"
version = "0.1.0"
description = ""
authors = [
    {name = "Maija Nevala"}
]
readme = "README.md"
requires-python = ">=3.13"

[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
gql = "^4.0.0"
python-dotenv = "^1.2.1"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
